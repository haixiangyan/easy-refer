mutation UpdateRefer($referForm: ReferForm!) {
    refer: updateRefer(referForm: $referForm) {
        referId
        refereeId
        refererId
        jobId
        resumeId
        status
        updatedAt
        # meta
        compnay
        refererName
        source
    }
}
