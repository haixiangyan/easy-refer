query GetReferItemList($userId: String!, $page: Int!) {
    referItemListPage: getReferItemList(userId: $userId, page: $page) {
        referItemList {
            referId
            jobId
            status
            updatedAt
            company
            refererName
            source
        }
        totalPages
    }
}
