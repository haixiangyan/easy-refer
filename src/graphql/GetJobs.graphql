query GetJobs($page: Int!) {
    jobsPage: getJobs(page: $page) {
        jobs {
            jobId
            refererId
            company
            requiredFields
            deadline
            expiration
            referredCount
            referTotal
            createdAt
            imageUrl
            source
            # meta
            refererName
        }
        totalPages
    }
}
