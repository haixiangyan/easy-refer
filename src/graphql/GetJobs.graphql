query GetJobs($page: Int!) {
    jobsPage: getJobs(page: $page) {
        jobs {
            jobId
            refererName
            company
            referredCount
            referTotal
            deadline
            expiration
            imageUrl
            source
            requiredFields
        }
        totalPages
    }
}
