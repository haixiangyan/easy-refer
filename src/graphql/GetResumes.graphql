query GetResumes($userId: String!, $page: Int!) {
    resumesPage: getResumes(userId: $userId, page: $page) {
        resumes {
            resumeId
            name
            createdAt
            experience
        }
        totalPages
    }
}
