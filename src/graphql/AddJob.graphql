mutation AddJob($userId: String!, $jobForm: JobForm!) {
    addJob(userId: $userId, jobForm: $jobForm) {
        jobId
        refererId
        company
        requiredFields
        deadline
        expiration
        referredCount
        referTotal
        createdAt
        imageUrl
        source
    }
}
