mutation UpdateUser($userForm: UserForm!) {
    user: updateUser(userForm: $userForm) {
        userId
        jobId
        email
        experience
        intro
        leetCodeUrl
        name
        phone
        resumeUrl
        thirdPersonIntro
    }
}
