mutation UpdateUser($userId: String!, $userForm: UserForm!) {
    user: updateUser(userId: $userId, userForm: $userForm) {
        userId
        jobId
        email
        experience
        intro
        leetCodeUrl
        name
        phone
        resumeUrl
        thirdPersonIntro
    }
}
