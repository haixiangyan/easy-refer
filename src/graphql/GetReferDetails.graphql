query GetReferDetails($referId: String!) {
    referDetails: getReferDetails(referId: $referId) {
        resumeId
        jobItem {
            jobId
            refererName
            company
            referredCount
            referTotal
            deadline
            expiration
            requiredFields
            createdAt
            source
            avatarUrl
        }
    }
}
