query GetJobItemList($page: Int!) {
    jobItemListPage: getJobItemList(page: $page) {
        jobItemList {
            jobId
            refererName
            company
            referredCount
            referTotal
            deadline
            expiration
            requiredFields
            createdAt
            source
            avatarUrl
        }
        totalPages
    }
}
