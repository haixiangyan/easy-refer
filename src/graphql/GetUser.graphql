query GetUser($userId: String!) {
    user: getUser(userId: $userId) {
        email
        experience
        intro
        jobId
        leetCodeUrl
        name
        phone
        resumeUrl
        thirdPersonIntro
        userId
    }
}
