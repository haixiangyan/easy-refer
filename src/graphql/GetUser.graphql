query GetUser($userId: String!) {
    user: getUser(userId: $userId) {
        userId
        name
        email
        phone
        experience
        resumeUrl
        intro
        jobId
        thirdPersonIntro
        leetCodeUrl
        avatarUrl
    }
}
