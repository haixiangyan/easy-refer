query GetRefers($userId: String!, $page: Int!) {
    refersPage: getRefers(userId: $userId, page: $page) {
        refers {
            referId
            jobId
            resumeId
            status
            company
            source
            experience
            refererName
            refereeName
            updatedAt
            createdAt
        }
        totalPages
    }
}
