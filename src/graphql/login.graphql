mutation login($loginForm: LoginForm!) {
    user: login(loginForm: $loginForm) {
        userId
        jobId
        email
        experience
        intro
        leetCodeUrl
        name
        phone
        resumeUrl
        thirdPersonIntro
    }
}
