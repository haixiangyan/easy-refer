mutation UpdateJob($userId: String!, $jobForm: JobForm!) {
    updateJob(userId: $userId, jobForm: $jobForm) {
        jobId
        company
        referer
        requiredFields
        deadline
        expiration
        referredCount
        referTotal
        imageUrl
        source
    }
}
