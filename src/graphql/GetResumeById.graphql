query GetResumeById($resumeId: String!) {
    resume: getResumeById(resumeId: $resumeId) {
        resumeId
        jobId
        createdAt
        referLinks

        userId
        name
        email
        experience
        intro
        phone
        leetCodeUrl
        thirdPersonIntro
        resumeUrl
    }
}
