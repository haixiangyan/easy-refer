query GetReferTable($userId: String!, $page: Int!) {
    referTable: getReferTable(userId: $userId, page: $page) {
        referRowList {
            referId
            jobId
            resumeId
            experience
            refereeName
            createdAt
        }
        totalPages
    }
}
