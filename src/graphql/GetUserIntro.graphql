query GetUserIntro($userId: String!) {
    userIntro: getUserIntro(userId: $userId) {
        avatarUrl
        name
        finishedRefers
        totalRefers
        finishedResumes
        totalResumes
    }
}
