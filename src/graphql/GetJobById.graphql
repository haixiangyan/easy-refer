query GetJobById($jobId: String!) {
    job: getJobById(jobId: $jobId) {
        jobId
        company
        requiredFields
        deadline
        expiration
        referredCount
        referTotal
        createdAt
        imageUrl
        source
    }
}
