query GetJobById($jobId: String!) {
    jobItem: getJobItem(jobId: $jobId) {
        jobId
        company
        requiredFields
        deadline
        expiration
        referredCount
        referTotal
        createdAt
        source
        avatarUrl
    }
}
