"Included scalars"
scalar JSON
scalar Upload

scalar Phone

type User {
    userId: String!
    name: String!
}

type Query {
    "Test query with a parameter"
    hello(name: String): String!
    getUserById(userId: String): User!
}

type Mutation {
    myMutation: String!

}

type Subscription {
    mySub: String!

}
